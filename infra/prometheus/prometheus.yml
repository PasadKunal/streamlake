global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "streamlake-serving"
    static_configs:
      - targets: ["host.docker.internal:8000"]

  # Phase 2: Flink metrics
  # - job_name: "flink"
  #   static_configs:
  #     - targets: ["flink-jobmanager:9249"]
