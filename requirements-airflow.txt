# Apache Airflow is best installed in an isolated environment or via Docker.
# Recommended: use the Astronomer Astro CLI (free tier) or Docker Compose setup.
# Manual install (if needed):
#   pip install "apache-airflow==2.9.1" --constraint \
#   "https://raw.githubusercontent.com/apache/airflow/constraints-2.9.1/constraints-3.11.txt"

apache-airflow==2.9.1
apache-airflow-providers-postgres==5.10.2
apache-airflow-providers-slack==8.6.2
